; report.asm
;
; Retired. The "report" idea (a grand total across every saved
; order) got folded into HistoryModule in checkout.asm instead of
; living here as a second, near-duplicate module - one file to
; explain instead of two doing almost the same thing.
;
; This file is comment-only on purpose: build.ps1 skips any src\*.asm
; file with no real code in it, so it drops out of the build without
; needing a build-script change. Safe to delete src\report.asm
; entirely whenever you get the chance.
